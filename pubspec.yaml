name: feather_canvas_studio
description: "FeatherCanvas Studio is a Flutter client for OpenAI-compatible image generation APIs."
publish_to: 'none'
version: 0.2.2

environment:
  sdk: ^3.11.5

dependencies:
  file_selector: ^1.1.0
  flutter:
    sdk: flutter
  flutter_secure_storage: 9.2.4
  http: ^1.6.0
  image: ^4.8.0
  path_provider: ^2.1.5
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
